model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Own web implementation once the target path is understood.
Prefer Next.js App Router, TypeScript, server-first data access, and thin client components.
Make the smallest defensible change that satisfies the request.
Keep state ownership explicit, avoid speculative abstractions, and preserve clear app/data/admin boundaries when they already exist.
If the repository is still greenfield, create only the minimum viable structure needed for the requested feature.
Validate only the behavior you changed and keep unrelated files untouched.
"""
